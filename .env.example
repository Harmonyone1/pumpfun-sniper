# Pump.fun Sniper Bot Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# Path to your Solana keypair JSON file
# WARNING: Use an isolated wallet with minimal SOL
# Set restrictive permissions: chmod 600 on Unix
KEYPAIR_PATH=/path/to/your/keypair.json

# RPC endpoint (use a dedicated provider, not public endpoints)
# Recommended: Helius, QuickNode, Triton
RPC_ENDPOINT=https://your-rpc-provider.com
RPC_WS_ENDPOINT=wss://your-rpc-provider.com

# Jito Block Engine (choose regional endpoint closest to your server)
# Options: ny, amsterdam, frankfurt, tokyo, singapore
JITO_BLOCK_ENGINE_URL=https://ny.mainnet.block-engine.jito.wtf

# ShredStream proxy (local)
SHREDSTREAM_GRPC_URL=http://127.0.0.1:10000

# ShredStream authentication keypair (for challenge-response)
# This is separate from your trading keypair
SHREDSTREAM_AUTH_KEYPAIR=/path/to/credentials/shredstream/shredstream-auth-keypair.json

# Optional: Jito API key for higher rate limits
# JITO_API_KEY=your-api-key-here

# Logging level (trace, debug, info, warn, error)
RUST_LOG=info,pumpfun_sniper=debug
